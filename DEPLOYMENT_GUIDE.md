# دليل النشر والتعديل على متجر فساتين العروسة

## 📦 محتويات المشروع

```
modern-multivendor-shop/
├── client/                 # الواجهة الأمامية (React + Tailwind)
│   ├── src/
│   │   ├── pages/         # جميع الصفحات
│   │   ├── components/    # المكونات المعاد استخدامها
│   │   ├── App.tsx        # التطبيق الرئيسي
│   │   └── index.css      # الأنماط العامة
│   └── index.html
├── server/                # الخادم (Express + tRPC)
│   ├── routers.ts         # جميع إجراءات tRPC
│   ├── db.ts              # دوال قاعدة البيانات
│   └── _core/             # ملفات النظام الأساسية
├── drizzle/               # قاعدة البيانات
│   └── schema.ts          # تعريف الجداول
├── package.json           # المكتبات والتبعيات
└── README.md              # هذا الملف
```

## 🚀 البدء السريع

### 1. تثبيت المتطلبات
```bash
# تثبيت Node.js و npm/pnpm
# ثم تثبيت المكتبات
pnpm install
```

### 2. إعداد قاعدة البيانات
```bash
# تطبيق الترحيلات
pnpm db:push

# أو إذا كنت تريد إعادة إنشاء قاعدة البيانات
pnpm db:reset
```

### 3. تشغيل المشروع
```bash
# تشغيل الخادم والواجهة الأمامية معاً
pnpm dev

# سيكون الموقع متاحاً على: http://localhost:3000
```

## 📝 التعديل على الموقع

### تعديل الصفحات
جميع الصفحات موجودة في `client/src/pages/`:
- `Home.tsx` - الصفحة الرئيسية
- `Products.tsx` - قائمة المنتجات
- `ProductDetail.tsx` - تفاصيل المنتج
- `Cart.tsx` - سلة التسوق
- `Checkout.tsx` - صفحة الدفع
- `Orders.tsx` - الطلبات
- `VendorDashboard.tsx` - لوحة البائع
- `AdminDashboard.tsx` - لوحة الإدارة

### تعديل الألوان والتصميم
الألوان والأنماط معرفة في `client/src/index.css`:
```css
@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 0 0% 3.6%;
    /* الألوان الأخرى... */
  }
}
```

### تعديل البيانات
البيانات مخزنة في قاعدة البيانات MySQL/TiDB.
لإضافة بيانات جديدة:
1. عدّل `drizzle/schema.ts` إذا كنت تريد تغيير الجداول
2. شغّل `pnpm db:push` لتطبيق التغييرات
3. أضف البيانات من خلال الواجهة أو مباشرة من قاعدة البيانات

### تعديل الإجراءات (API)
جميع الإجراءات معرفة في `server/routers.ts`:
```typescript
export const appRouter = router({
  products: router({
    list: publicProcedure.query(async () => {
      // الكود هنا
    }),
    // إجراءات أخرى...
  }),
  // موجهات أخرى...
});
```

## 🎨 تخصيص المتجر

### تغيير اسم المتجر
1. عدّل `VITE_APP_TITLE` في متغيرات البيئة
2. عدّل شعار المتجر في `Navigation` في `App.tsx`

### تغيير الألوان الرئيسية
ابحث عن `rose-600` في الملفات واستبدلها بلونك المفضل:
- `rose-600` → `blue-600` (أزرق)
- `rose-600` → `purple-600` (بنفسجي)
- `rose-600` → `green-600` (أخضر)

### إضافة منتجات جديدة
يمكنك إضافة منتجات من خلال:
1. لوحة تحكم البائع (Vendor Dashboard)
2. أو مباشرة من قاعدة البيانات

## 🔌 تكامل الدفع (Stripe)

### إضافة مفاتيح Stripe
1. أنشئ حساب Stripe: https://stripe.com
2. احصل على المفاتيح من Dashboard → Developers → API Keys
3. أضفها في متغيرات البيئة:
   - `STRIPE_SECRET_KEY`
   - `VITE_STRIPE_PUBLISHABLE_KEY`

### تفعيل الدفع
عدّل `server/routers.ts` وأضف إجراء الدفع:
```typescript
checkout: protectedProcedure
  .input(z.object({ items: z.array(z.object({ productId: z.number(), quantity: z.number() })) }))
  .mutation(async ({ input, ctx }) => {
    // كود معالجة الدفع
  })
```

## 📧 إرسال البريد الإلكتروني

لإرسال رسائل بريد إلكترونية للعملاء:
```typescript
import { notifyOwner } from "./server/_core/notification";

await notifyOwner({
  title: "طلب جديد",
  content: "تم استقبال طلب جديد من العميل"
});
```

## 🧪 الاختبار

### تشغيل الاختبارات
```bash
pnpm test
```

### كتابة اختبارات جديدة
الاختبارات موجودة في ملفات `.test.ts`:
```typescript
import { describe, it, expect } from "vitest";

describe("Product", () => {
  it("should get product by id", async () => {
    // الاختبار هنا
  });
});
```

## 🚢 النشر

### النشر على Manus
1. انقر على زر "Publish" في لوحة التحكم
2. اختر الفرع (main)
3. انتظر حتى ينتهي النشر

### النشر على خادم خاص
```bash
# بناء المشروع
pnpm build

# تشغيل الخادم
pnpm start
```

## 📱 الميزات الرئيسية

✅ نظام مصادقة متكامل (OAuth)
✅ إدارة المنتجات والفئات
✅ سلة التسوق والدفع
✅ لوحة تحكم البائعين
✅ لوحة تحكم الإدارة
✅ نظام الطلبات والتتبع
✅ نظام المراجعات والتقييمات
✅ نظام الإشعارات
✅ دعم اللغة العربية
✅ تصميم متجاوب (Responsive)

## 🐛 استكشاف الأخطاء

### المشروع لا يعمل
```bash
# حذف node_modules وإعادة التثبيت
rm -rf node_modules
pnpm install
pnpm db:push
pnpm dev
```

### خطأ في قاعدة البيانات
```bash
# إعادة تعيين قاعدة البيانات
pnpm db:reset
```

### خطأ في البناء
```bash
# التحقق من TypeScript
pnpm check

# إعادة بناء المشروع
pnpm build
```

## 📚 الموارد المفيدة

- [React Documentation](https://react.dev)
- [Tailwind CSS](https://tailwindcss.com)
- [tRPC Documentation](https://trpc.io)
- [Drizzle ORM](https://orm.drizzle.team)
- [Express.js](https://expressjs.com)

## 💬 الدعم

للحصول على المساعدة:
1. تحقق من الوثائق أعلاه
2. اطلع على الأخطاء في console
3. تحقق من ملفات السجل في `.manus-logs/`

---

**آخر تحديث:** 22 يناير 2026
**الإصدار:** 1.0.0
